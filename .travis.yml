#
# Get ghdl and run all the test benches
#
language: C

sudo: false
#dist: trusty

notifications:
  email:
    on_success: change
    on_failure: change

addons:
  apt:
    packages:
    - libftdi1-dev
    - libqt4-dev
    - qt4-qmake

script:
  - .travis/build.sh

